{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/js/bundle.js",
    "clean": "rimraf dist/js output",
    "postinstall": "psc-package install",
    "repl": "pulp psci",
    "start": "pulp --watch build --to dist/js/bundle.js",
    "test": "pulp test"
  },
  "devDependencies": {
    "psc-package": "^0.3.2",
    "pulp": "^12.3.0",
    "purescript": "^0.12.0",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  }
}
