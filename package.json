{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/app.js",
    "postinstall": "psc-package install",
    "start": "pulp --watch build --to dist/app.js",
    "update": "psc-package updates"
  },
  "devDependencies": {
    "psc-package": "^0.3.2",
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0",
    "rimraf": "^2.6.2"
  }
}
