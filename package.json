{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/app.js",
    "postinstall": "./bin/psc-package update",
    "start": "pulp --watch build --to dist/app.js"
  },
  "devDependencies": {
    "pulp": "^12.0.1",
    "purescript": "^0.11.7",
    "purescript-psa": "^0.6.0"
  }
}
